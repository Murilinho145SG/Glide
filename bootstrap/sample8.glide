fn main() -> int {
    let name: string = "world";
    let n: int = 42;
    let ok: bool = true;
    println!("hello,", name, "answer=", n, "ok=", ok);
    print!("loop: ");
    for let i: int = 0; i < 3; i++ {
        print!(i, " ");
    }
    println!("");
    return 0;
}
