fn main() -> int {
    let x: int = 2 + 4;
    println!(x, 123, "example");
    println!("name:", "Glide", "version:", 1, 3.14, true);

    print!("countdown: ");
    for let i: int = 3; i > 0; i-- {
        print!(i, " ");
    }
    println!("done");

    return 0;
}
