// Pure opaque type: Glide promises Foo exists, gcc will resolve at link.

extern type MyHandle;

fn main() -> int {
    let h: *MyHandle = malloc(64) as *MyHandle;
    if h == null {
        return 1;
    }
    println!("got opaque handle at:", h);
    free(h as *void);
    return 0;
}
