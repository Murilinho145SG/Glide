// Stress test for work-stealing — many short-lived coros so workers
// constantly migrate work between themselves.

fn worker(id: int) {
    println!("worker", id);
}

fn main() -> int {
    for i in 0..20 {
        spawn worker(i);
    }
    return 0;
}
