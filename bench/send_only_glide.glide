fn producer(c: chan<int>, n: int) {
    for i in 0..n {
        c.send(i);
    }
}
fn main() -> int {
    let n: int = 100000;
    let c: chan<int> = make_chan(1024);
    spawn producer(c, n);
    for i in 0..n {
        let _v: int = c.recv();
    }
    return 0;
}
