// Pure spawn cost: spawn N coros that do almost nothing, no chan, no sync.

fn worker() {
    return;
}

fn main() -> int {
    let n: int = 100000;
    for i in 0..n {
        spawn worker();
    }
    return 0;
}
